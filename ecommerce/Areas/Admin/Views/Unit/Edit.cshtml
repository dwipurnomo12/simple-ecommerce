@model ecommerce.Models.ViewModels.UnitViewModel
@{
    ViewData["Title"] = "Edit Unit";
}

<div id="edit_unit_modal" class="modal fade" tabindex="-1" role="dialog" aria-labelledby="addUnitLabel"
     aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            
            <!-- Header -->
            <div class="modal-header modal-colored-header bg-primary">
                <h4 class="modal-title" id="addUnitLabel">Add New Unit</h4>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>

            <!-- Body -->
            <div class="modal-body">
                <form method="post">
                    @Html.AntiForgeryToken()
                    
                    <input type="hidden" id="unit_id">

                    <div class="form-group mb-3">
                        <label for="Name_edit" class="form-label">Unit Name</label>
                        <input type="text" class="form-control" id="Name_edit" required>
                        <span id="Name_edit-error" class="text-danger"></span>
                    </div>

                    <!-- Footer -->
                    <div class="modal-footer">
                        <button type="button" class="btn btn-light" data-bs-dismiss="modal">Close</button>
                        <button type="submit" id="Update" class="btn btn-primary">Save changes</button>
                    </div>
                </form>
            </div>

        </div><!-- /.modal-content -->
    </div><!-- /.modal-dialog -->
</div><!-- /.modal -->

